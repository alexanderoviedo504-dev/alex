# Carpeta de videos

Coloca aquí tus archivos de video (por ejemplo `.mp4`, `.webm`, `.ogg`).

Asegúrate de que los nombres coincidan con la propiedad `"video"` de cada elemento en `data/videos.json`.
